dist: xenial
language: python
python:
  - "3.8"

install:
  - pip install -r requirements/ci.txt

env:
  - DJANGO_SETTINGS_MODULE: config.settings.ci
  - DEBUG: True
  - SECRET_KEY: "tests"

script:
  - make test